mixin reviewCard(review)
  .reviews__card
    .reviews__avatar
      img.reviews__avatar-img(src=`/images/users/${review.user.photo}`)
      h6.reviews__user= review.user.name
    p.reviews__text= review.review
    .reviews__rating
        h6.reviews__user= `Rating: ${review.rating}`
         